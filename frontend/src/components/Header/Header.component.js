import React from 'react';
import { Navbar } from 'react-bootstrap';

const Header = () => (
    <Navbar className="bg-orange" variant="dark">
        <Navbar.Brand>Secret Santa !</Navbar.Brand>
    </Navbar>
)

export default Header;